<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
    <div class="container">
        <h1 class="display-5">Administrator's Alert Panel</h1>
        <div class="mb-3">
            <label for="message" class="form-label">What is the message you want everyone know?</label>
            <textarea class="form-control" id="message" rows="3"></textarea>
        </div>

        <div class="form-floating">
            <select class="form-select" id="floatingSelect" aria-label="Floating label select example">
                <option value="update" selected>Feature Update</option>
                <option value="alert">Alert</option>
            </select>
            <label for="floatingSelect">Message type</label>
        </div>
        <button class="btn btn-outline-success" style="margin-top: 10px;">Publish</button>
    </div>

    <script>
        document.querySelector('.btn').addEventListener('click', async function(e) {
            e.preventDefault()

            let message = document.querySelector('#message').value
            let type = document.querySelector('#floatingSelect').value

            let response = await fetch(`/message?message=${message}&type=${type}`)
            let data = await response.json()
            
            window.location.href = data.url
        })
    </script>
</body>
</html>